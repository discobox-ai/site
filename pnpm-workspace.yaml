# Not a workspace — this file carries the build allowlist pnpm reads from the
# project root. esbuild is Astro's bundler and has to run its install script.
allowBuilds:
  esbuild: true
  workerd: true
minimumReleaseAgeExclude:
  - miniflare@5.20260831.0-alpha
  - wrangler@4.128.0
